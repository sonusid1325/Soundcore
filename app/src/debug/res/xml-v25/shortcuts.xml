<?xml version="1.0" encoding="utf-8"?>
<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">
    <shortcut
        android:enabled="true"
        android:icon="@drawable/shortcut_search"
        android:shortcutId="search"
        android:shortcutShortLabel="@string/search">
        <intent
            android:action="app.sonusid.soundcore.action.SEARCH"
            android:targetClass="app.sonusid.soundcore.MainActivity"
            android:targetPackage="app.sonusid.soundcore.debug" />
    </shortcut>
    <shortcut
        android:enabled="true"
        android:icon="@drawable/shortcut_library"
        android:shortcutId="library"
        android:shortcutShortLabel="@string/filter_library">
        <intent
            android:action="app.sonusid.soundcore.action.LIBRARY"
            android:targetClass="app.sonusid.soundcore.MainActivity"
            android:targetPackage="app.sonusid.soundcore.debug" />
    </shortcut>
</shortcuts>
